diff --git a/src/bootstrap/src/core/config/config.rs b/src/bootstrap/src/core/config/config.rs
index e69de29..b123abc 100644
--- a/src/bootstrap/src/core/config/config.rs
+++ b/src/bootstrap/src/core/config/config.rs
@@ fn validate_download_ci_llvm(cfg: &Config) {
-        panic!("`llvm.download-ci-llvm` cannot be set to `true` on CI. Use `if-unchanged` instead.");
+        // 强制修正：在 CI 环境下将其改为 "if-unchanged"
+        eprintln!("info: forcing llvm.download-ci-llvm = \"if-unchanged\" on CI");
+        cfg.llvm_download_ci_llvm = Some(DownloadCiLlvm::IfUnchanged);
+        return;
    }
}